generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma" // caminho customizado para o backend
}

datasource db {
  provider = "postgresql"
  // DATABASE_URL será pego do .env
}

// Model de Exemplo para o Cardápio
model ItemCardapio {
  id         String   @id @default(uuid())
  nome       String
  descricao  String
  preco      Decimal
  categoria  String

  @@map("itens_cardapio")
}
